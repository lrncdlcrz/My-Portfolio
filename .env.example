# EmailJS (https://www.emailjs.com), used by the Contact page form.
# Create a free account, add an email service + template, then fill these in
# your own .env.local (never commit real keys).
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=
